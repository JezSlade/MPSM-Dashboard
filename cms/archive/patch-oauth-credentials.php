<?php
/**
 * Archived placeholder.
 *
 * The original one-off patch script embedded production OAuth values. It has
 * been replaced so credentials are only managed through .env/server secrets.
 */

fwrite(STDERR, "This archived credential patch has been disabled. Update .env/server secrets instead.\n");
exit(1);
