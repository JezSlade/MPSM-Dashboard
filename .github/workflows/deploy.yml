name: FTP Deploy - Bulletproof

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 🔍 Checkout Code
        uses: actions/checkout@v4

      - name: 📦 Prepare Files
        run: |
          echo "=== 🗂️ Files to Deploy ==="
          ls -la
          echo "========================="

      - name: 🚀 FTP Deploy (Verbose)
        uses: sebastianpopp/ftp-action@releases/v2
        with:
          host: "ftp.resolutionsbydesign.us"
          user: "mpsm@resolutionsbydesign.us"
          password: "MP$M_Nr0lr"  # 🔴 REMOVE AFTER TESTING
          localDir: "./"
          remoteDir: "/home/resolut7/public_html/resolutionsbydesign.us/"
          options: "--delete --transfer-all --verbose --no-perms --timeout=30"

      - name: ✅ Verify Completion
        run: |
          echo "Deployment completed without errors"
          echo "Check your site: https://resolutionsbydesign.us"