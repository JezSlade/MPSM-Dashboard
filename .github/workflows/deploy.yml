name: FTP Deploy - Final Lean

on:
  push:
    branches: ["main"]  # Trigger on pushes to the 'main' branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 🔍 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔢 Generate Version File
        run: |
          echo "window.appVersion = \"0.0.0.${GITHUB_RUN_NUMBER}\";" > public/version.js
        env:
          GITHUB_RUN_NUMBER: ${{ github.run_number }}

      - name: 🚀 Deploy Website via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.0
        with:
          server: ftp.resolutionsbydesign.us
          username: mpsm@mpsm.resolutionsbydesign.us
          password: Deploy123!
          protocol: ftp
          port: 21

          # Note: trailing slash required
          local-dir: ./public/

          # Deploy into the root of your site
          server-dir: /

          # Remove remote files not present locally
          dangerous-clean-slate: true

          # Exclude source files outside public/ and GitHub metadata
          exclude: |
            AllEndpoints.json
            .github/**
          
          log-level: verbose

      - name: 📜 Deployment Completed Report
        run: |
          echo "### DEPLOYMENT SUMMARY ###"
          echo "- Version: 0.0.0.${GITHUB_RUN_NUMBER}"
          echo "- FTP Server: ftp.resolutionsbydesign.us"
          echo "- Server Directory: /"
          echo "- Local Directory: ./public/"
        env:
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
